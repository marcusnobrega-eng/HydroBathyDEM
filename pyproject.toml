[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hydrobathydem"
version = "0.1.0"
description = "River-aware DEM conditioning, D4 routing diagnostics, and Lin2020 hydraulic geometry calibration for flood models."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [
    {name = "Marcus Gomes"},
]
keywords = [
    "DEM",
    "FABDEM",
    "HydroPol2D",
    "river conditioning",
    "hydraulic geometry",
    "river bathymetry",
    "D4 routing",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: GIS",
]
dependencies = [
    "affine",
    "geopandas",
    "matplotlib",
    "numpy",
    "pandas",
    "pyogrio",
    "pyproj",
    "rasterio",
    "scipy",
    "shapely",
    "whitebox",
]

[project.urls]
Repository = "https://github.com/marcusnobrega-eng/HydroBathyDEM"
Issues = "https://github.com/marcusnobrega-eng/HydroBathyDEM/issues"

[project.optional-dependencies]
dev = [
    "build",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[project.scripts]
hydrobathydem-condition-1000m = "dem_processing.run_conditioning_1000m:main"
hydrobathydem-condition = "dem_processing.condition_dem:main"
hydrobathydem-prepare-lin2020 = "dem_processing.prepare_lin2020_bankfull_geometry:main"
hydrobathydem-calibrate-hydraulics = "dem_processing.calibrate_spatial_hydraulic_geometry:main"
hydrobathydem-preflight = "dem_processing.preflight:main"
